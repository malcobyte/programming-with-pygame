This week's exercises are about the `read_movement()` function itself,
not a single script. For each exercise, make a subfolder (e.g.
`exercise1/`) containing your own copy of `my_input_handler.py` plus a
small demo script, so each exercise stays independent. See
`../ASSIGNMENT.md` for what each one should do.
