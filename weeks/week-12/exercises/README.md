This week's exercises are about the `slice_sheet()` function itself.
For each exercise, make a subfolder (e.g. `exercise1/`) with your own
copy of `my_sprite_loader.py` plus whatever sheet/demo files that
exercise needs. See `../ASSIGNMENT.md` for what each one should do.
