Build your "Own Your Sprite Loader" project here as `main.py`, using
both `my_sprite_loader.py` and Week 11's `my_input_handler.py`
(copy both files into this folder). See `../ASSIGNMENT.md` for the
full requirements. The `walker.png` sprite sheet is already copied
into this folder for you.
